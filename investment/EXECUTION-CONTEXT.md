# 执行上下文（当前组合 + 信念 + 历史决策）[TEMPLATE]

> **本文件 = 执行模式专用上下文。** CLAUDE.md 不 @import 它，默认会话不加载，避免污染白纸构建/独立审核/个股研究。
>
> **谁读：**
> - `/sop` `/buy` `/risk` `/scan` slash command 显式读
> - 月度执行 / 周风控 / 季度扫描 / 紧急审核会话
>
> **谁不读：**
> - `/research <Q>` `/audit <T>` `/greenfield` 触发的 subagent（明令禁止）
> - 主 Claude 在"独立看 X""白纸构建"等自然语言触发后

---

## 1. 当前组合（v<N>）

> 权威数据以 `investment/portfolio-versions.md` 和 `investment/portfolio-config.txt` 为准。组合变更时必须同步更新此文件。

```
<TICKER>  = <amount> (<X>%)  <说明>
<TICKER>  = <amount> (<X>%)  <说明>
...
合计      = <total>/月
```

层级：T1 核心 <X>% / T2 成长 <X>% / T3 对冲 <X>% / T4 现金 <X>%

---

## 2. v<N> 审核结论（YYYY-MM-DD）

| 持仓 | 判定 | 评分 | 核心数据 |
|------|------|------|---------|
| <TICKER> | PASS / CONDITIONAL / FAIL | ✅ / ⚠️ / ❌ | <关键数据> |

**组合总评：<X>/100，<PASS / CONDITIONAL PASS / FAIL>，<可执行 / 需调整 / 暂停>。**

---

## 3. 我的投资信念（不轻易动摇）

> 列出 3-6 条对当前组合的核心信念，每条一句话 + 一个数据支撑。
> 这些信念是"不会因为用户随口一提就动摇"的底线。

- **<信念 1>**：<一句话理由 + 数据>
- **<信念 2>**：<一句话理由 + 数据>
- **DCA 不择时**：每月固定日无脑买，不看盘、不猜底、不追高
- **小组合每个仓位都珍贵**：持仓数量少，不容许冗余和重叠

---

## 4. 历史决策记录 [APPEND ONLY]

| 日期 | 决策 | 原因 |
|------|------|------|
| YYYY-MM-DD | 初始组合 v0 | 首次建仓 |
| YYYY-MM-DD | <决策内容> | <原因> |

> 只记录真决策（换股 / 调比例 / 紧急审核 / 里程碑 / 规则变更），按模板正常定投不写。
